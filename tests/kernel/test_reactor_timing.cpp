// PLACEHOLDER — registered in tests/CMakeLists.txt by Phase 0 so the reactor
// unit can replace this file without touching a shared build file.
//
// The suite is named ReactorTiming because tests/CMakeLists.txt labels that
// suite "timing", so a loaded machine can exclude it with `ctest -LE timing`.

// clang-format off
#include <gtest/gtest.h>
// clang-format on

namespace
{

    TEST( ReactorTiming,
          Placeholder )
    {
        SUCCEED();
    }

}    // namespace
